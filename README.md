# datn_full
## ĐATN
## Cài đặt trước các thư viện 
`pip install -r requirements.txt`
## Cài đặt thêm thư viện trong strongsort
` cd StrongSORT-YOLO `
`pip install -r requirements.txt`

## Run source
```bash

python inference.py


```
